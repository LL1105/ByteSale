server:
  port: 9001

spring:
  application:
    name: gateway-server
  profiles:
    active: test
  cloud:
    nacos:
      config:
        file-extension: yaml
        shared-configs:
          -${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}